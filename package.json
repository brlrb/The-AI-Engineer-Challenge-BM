{
  "name": "beyond-chatgpt",
  "version": "0.1.0",
  "private": true,
  "description": "A GPT-like developer assistant with FastAPI backend and Next.js frontend",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "install:frontend": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
